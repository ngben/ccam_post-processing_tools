Use these files to concatenate daily/monthly data after processing with axiom

Use `--run` flag in the .pbs files to modify the data. Without the `--run` flag no data will be modified. 
e.g., `python concat_monthly.py "$DIR" --run`